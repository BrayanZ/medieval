#!/usr/bin/env ruby

require 'medieval'

folder = ARGV[0] ? ARGV[0] : "spec"

Dir[Dir.pwd + "/#{folder}/**/*.rb"].each do |file| 
  if file.end_with? "_spec.rb"
    puts file
    require file
  end
end
